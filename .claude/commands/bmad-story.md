Draft a story for this task using the project template.

Task:
$ARGUMENTS

Instructions:
1. Use `bmad/templates/story-template.md`.
2. Fill all sections with concrete paths, interfaces, and acceptance criteria.
3. Ensure the story references one primary domain from `component-map.json`.
4. Include explicit no-lookahead/risk checks when touching execution logic.

Output only the completed story markdown.
